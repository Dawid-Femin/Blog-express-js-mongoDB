extends ../layout

block content
  h1= title
  table 
    tr 
      th id
      th tytul
      th data dodania
      th akcja
    each item, index in data 
      tr 
        td=index
        td=item.title 
        td=item.created
        td 
          a(class="secondBtn" href=`/admin/news/delete/${item._id}`) Usuń